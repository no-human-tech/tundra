# Tundra pnpm configuration
# Keep dependency resolution strict and reproducible.
auto-install-peers=true
strict-peer-dependencies=false
# Hoist only what is needed; keeps the dependency graph honest across workspaces.
shamefully-hoist=false
# Use the workspace protocol for internal packages.
link-workspace-packages=true
prefer-workspace-packages=true
